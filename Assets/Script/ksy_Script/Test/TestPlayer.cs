using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.InputSystem;
using UnityEngine.InputSystem.Android;

public class TestPlayer : TestBase
{
   /* CharacterBase player;
    ItemInventoryWindowExplanRoom item;
    private void Start()
    {
        player = FindObjectOfType<CharacterBase>();
        item = FindObjectOfType<ItemInventoryWindowExplanRoom>();
    }

    protected override void OnEnable()
    {
        base.OnEnable();
        item.onChangeHp += OnUpgradeHp;
    }

    protected override void Test1(InputAction.CallbackContext _)
    {
        OnUpgradeHp(20);
    }*/

    /*void OnUpgradeHp(int hp)
    {
        if (player.hp > 0 && player.hp <1001 - hp)
        {
            player.hp = player.hp + hp;
            Debug.Log(player.hp);
        }
    }*/
}
